# 🏗️ Loading States Architecture

Visual architecture and component hierarchy.

## 📐 Component Hierarchy

```
┌─────────────────────────────────────────────────────────────┐
│                     Loading States System                    │
└─────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┼─────────────┐
                │             │             │
         ┌──────▼──────┐ ┌───▼────┐ ┌─────▼──────┐
         │  Core Layer │ │ Content│ │ Interactive│
         │             │ │ Layer  │ │   Layer    │
         └──────┬──────┘ └───┬────┘ └─────┬──────┘
                │            │            │
    ┌───────────┼────┐   ┌───┼─────┐   ┌──┼──────────┐
    │           │    │   │   │     │   │  │          │
┌───▼───┐  ┌───▼──┐ │   │   │     │   │  │          │
│Shimmer│  │Skeleton│ │   │   │     │   │  │          │
│       │  │  Base  │ │   │   │     │   │  │          │
└───────┘  └────────┘ │   │   │     │   │  │          │
                      │   │   │     │   │  │          │
           ┌──────────▼───▼───▼─────▼───▼──▼──────┐   │
           │                                       │   │
           │  StoriesListSkeleton                 │   │
           │  StoryDetailSkeleton                 │   │
           │  WizardSkeletons                     │   │
           │  SearchResultsSkeleton               │   │
           │                                       │   │
           └───────────────────────────────────────┘   │
                                                       │
                ┌──────────────────────────────────────▼─┐
                │                                         │
                │  LoadingButton                          │
                │  AsyncButton                            │
                │  PullToRefreshCustom                    │
                │  InlineLoadingStates                    │
                │                                         │
                └─────────────────────────────────────────┘
```

## 🔄 Data Flow

```
┌──────────────┐
│ User Action  │
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│ Set isLoading    │
│ = true           │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐     ┌─────────────────┐
│ Show Skeleton/   │────▶│ Shimmer Effect  │
│ Loading State    │     │ Animates        │
└──────┬───────────┘     └─────────────────┘
       │
       ▼
┌──────────────────┐
│ Fetch Data from  │
│ API/Cache        │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Data Received    │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Set isLoading    │
│ = false          │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐     ┌─────────────────┐
│ Transition to    │────▶│ Fade + Scale    │
│ Actual Content   │     │ Animation       │
└──────────────────┘     └─────────────────┘
```

## 🎨 Shimmer System

```
┌─────────────────────────────────────────────────────┐
│                    ShimmerModifier                   │
│                                                      │
│  1. Create gradient with 3 stops:                   │
│     [Clear → White.opacity(0.4) → Clear]            │
│                                                      │
│  2. Rotate -45° for diagonal sweep                  │
│                                                      │
│  3. Animate from left to right (0% → 100%)          │
│                                                      │
│  4. Duration: 1.5s linear, infinite                 │
│                                                      │
│  5. Apply as overlay with .blendMode(.overlay)      │
│                                                      │
└─────────────────────────────────────────────────────┘
                         │
                         │ Applied to ▼
                         │
    ┌────────────────────┼─────────────────────┐
    │                    │                     │
┌───▼────┐        ┌──────▼────┐        ┌──────▼────┐
│Skeleton│        │Skeleton   │        │Skeleton   │
│ Line   │        │ Image     │        │ Badge     │
└────────┘        └───────────┘        └───────────┘
```

## 📦 Component Dependencies

```
ShimmerModifier (Core)
    └── Used by ▼
        ├── SkeletonView (Base)
        │   └── Used by ▼
        │       ├── SkeletonLine
        │       ├── SkeletonImage
        │       ├── SkeletonBadge
        │       └── All Content Skeletons
        │
        ├── GradientLinearProgress
        └── InlineImageLoading

SkeletonView (Base)
    └── Used by ▼
        ├── StoryCardSkeleton
        ├── StoryDetailSkeleton
        ├── WizardSkeletons
        └── SearchResultsSkeleton

CustomProgressViews (Loaders)
    └── Used by ▼
        ├── LoadingStatesShowcase
        ├── AnalyzingStepSkeleton
        ├── SearchingStateSkeleton
        └── LoadingStateView

AccessibleLoadingView
    └── Wraps ▼
        └── All CustomProgressViews
            (with reduce motion support)
```

## 🎯 Usage Patterns

### Pattern 1: Full Page Loading
```
┌─────────────────────┐
│   View Model        │
│   isLoading: Bool   │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│   View              │
│   if isLoading {    │
│     Skeleton()      │
│   } else {          │
│     Content()       │
│   }                 │
└─────────────────────┘
```

### Pattern 2: Inline Loading
```
┌─────────────────────┐
│   HStack {          │
│     Label()         │
│     InlinePlaceholder(
│       isLoading,    │
│       content: Data │
│     )               │
│   }                 │
└─────────────────────┘
```

### Pattern 3: Progressive Loading
```
┌─────────────────────┐
│   VStack {          │
│     Header ✓        │
│                     │
│     if loading1 {   │
│       Skeleton1     │
│     } else {        │
│       Content1 ✓    │
│     }               │
│                     │
│     if loading2 {   │
│       Skeleton2     │
│     } else {        │
│       Content2      │
│     }               │
│   }                 │
└─────────────────────┘
```

## 🌊 Animation Timeline

```
0ms          300ms        800ms        1500ms       1800ms
│────────────│────────────│────────────│────────────│
│            │            │            │            │
Skeleton     Item 1       Item 2       Shimmer      Content
appears      fades in     fades in     completes    loaded
             (stagger)    (stagger)    full sweep   
│                                                   │
│◄──────────── Loading State ─────────────────────►│
                                                    │
                                                    ▼
                                            ┌───────────────┐
                                            │ Fade + Scale  │
                                            │ to Content    │
                                            │ (300ms)       │
                                            └───────────────┘
```

## 🎨 Color Adaptation

```
┌─────────────────────────────────────────────┐
│          Environment.colorScheme            │
└─────────────┬───────────────────────────────┘
              │
        ┌─────▼─────┐
        │  .light   │
        └─────┬─────┘
              │
    ┌─────────┴──────────┐
    │                    │
    ▼                    ▼
Base: Gray.opacity(0.15)  Shimmer: White.opacity(0.4)


        ┌─────▼─────┐
        │  .dark    │
        └─────┬─────┘
              │
    ┌─────────┴──────────┐
    │                    │
    ▼                    ▼
Base: Gray.opacity(0.2)   Shimmer: White.opacity(0.15)
```

## ♿ Accessibility Flow

```
┌─────────────────────────────────────────┐
│  Environment.accessibilityReduceMotion  │
└─────────────┬───────────────────────────┘
              │
        ┌─────▼─────┐
        │   false   │
        └─────┬─────┘
              │
              ▼
    ┌──────────────────┐
    │ Full Animation   │
    │ • Shimmer sweep  │
    │ • Rotation       │
    │ • Spring bounce  │
    └──────────────────┘


        ┌─────▼─────┐
        │   true    │
        └─────┬─────┘
              │
              ▼
    ┌──────────────────┐
    │ Reduced Motion   │
    │ • Static overlay │
    │ • No shimmer     │
    │ • Simple fade    │
    └──────────────────┘
```

## 🏭 Factory Pattern

```swift
// LoadingStateFactory (Conceptual)
enum LoadingContext {
    case storiesList(viewMode: ViewMode)
    case storyDetail
    case wizardStep(step: WizardStep)
    case search
    case inline(type: InlineType)
}

func makeLoadingView(for context: LoadingContext) -> some View {
    switch context {
    case .storiesList(let viewMode):
        return StoriesListSkeleton(viewMode: viewMode)
    case .storyDetail:
        return StoryDetailSkeleton()
    // ... etc
    }
}
```

## 📊 Performance Optimization

```
┌─────────────────────┐
│  Scroll View        │
│  with LazyVStack    │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Viewport Window    │
│  ┌───────────────┐  │
│  │ Skeleton 1 ✓  │  │  ◄─ Rendered
│  │ Skeleton 2 ✓  │  │  ◄─ Rendered
│  │ Skeleton 3 ✓  │  │  ◄─ Rendered
│  └───────────────┘  │
│  Skeleton 4         │  ◄─ Not rendered (offscreen)
│  Skeleton 5         │  ◄─ Not rendered (offscreen)
│  Skeleton 6         │  ◄─ Not rendered (offscreen)
└─────────────────────┘

Only visible skeletons are rendered!
Shimmer animations paused when offscreen.
```

---

**Architecture designed for performance, accessibility, and delight!** 🏗️✨
